1배개변수의 다형성에 대해서 고민을 많이 가졌다.driver.drive(bus)에서 driver와 bus의 생성자를 만들고 interface로 상속받은후 public void drive(Vehicle vehicle);에서 Vehicle vehicle은 결국 bus와 같기때문에 new Bus()처럼 
생각해 이해를 하였는데 자세한 내용을 블로그에 코드와 예시를 들며 설명해 놓았다.
2 제네릭에서 static을 못쓰는데 타입변수가 인스턴스로 간주되기 때문에 정적변수와 다르다는 내용을 다시한번 정리했고 간단하게 static 변수와 instance변수 차이점을 다음과 같이 적었다. static 인 정적변수는 선언시 초기화가 되면(생성자 못만듬),정적블록을 사용하지만 인스턴스 변수는 생성자 또는 선언시 초기화가된다.
