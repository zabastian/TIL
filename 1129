인터페이스를 이용한 다형성,의존성주입,제어의 역전까지 강의자료를 정리하였다.정말 놀라웠다.
어떻게 부모 객체에 있는 객체를 이용해 쉽게 값을 받는지 신기했다.

이해한 내용 :
this.engine = new GasEngine()에서 내부에서 값을 받아 초기화 해주었다 생각하면 되는거야? 
외부에서 초기화한 필드의  코드는 car1.name = "MyCar";로 인스턴스화 해줬다 볼 수 있잖아
car1을 인스턴스화 해주는 필드라고 생각할 때 그럼 저 내부에서 초기화한 코드는
GasEngine을 인스턴스화한 필드.engine = "내부에서 생성할 어떠한 값";이렇게 해석 
