팀프로젝트 하느라 TIL 내용을 notion에다 적었다 앞으로 2군데 다 기록할 예정이다.

팀원과 논의중 
jpql,querydsl 중에 querydsl로 하라는지 이해가 좀 안되었느데,
JPQL은 문자열 기반이기 때문에 컴파일 시 오류를 잡을 수 없음
반면 QueryDSL은 타입 안전성(type safety)이 보장되므로, 컴파일 시 오류를 확인할 수 있다고 해 좀 복잡해질 경우 querydsl을 사용하자는 의견이 나왔다




자바 문제 풀이중 
            // return Character.toString(s.charAt(num/2-1)) + Character.toString(s.charAt(num/2));

이렇게 문제를 풀었는데 생각해보니 substring을 사용해도 되곘다 생각해서 이렇게 고쳤다.
            return s.substring(num/2-1,num/2+1);
