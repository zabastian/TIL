 HashSet<String> send = map.get(id_list[i]);를 쓰는이유는

 중복 없이, 순서 보장 하지 않게 하기 위해서이다.

 LinkedSet이나 TreeSet이 오게 되면 순서보장하거나, 자동정렬을 하기 때문에

 Set<String> send ~~ 아렇게 사용해도 결과는 똑같나 확실하게 표시해주기 위해 HashSet을 사용했다.
