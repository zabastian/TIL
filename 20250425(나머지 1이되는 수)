나머지 1이 되는수 라는 문제에서 return 값이 int라는 정수형이였는데 만족하지 않으면 -1을 반환하는 이유가 궁금해 찾아보았다.

보통 -1은 "에러"나 "찾을 수 없음" 같은 걸 표현할 때 많이 쓰는데

예를 들어, Java에서 .indexOf() 함수도 원하는 걸 못 찾으면 -1을 반환한다.

그래서 return -1;은 일반적으로 "조건을 만족하는 걸 못 찾았을 때"를 의미하는 패턴이라고 약속했기 때문에 이렇게 사용한다고 생각을 해야한다.
